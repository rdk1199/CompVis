#include "logistic_classifier.h"

#include "../Math/functions.h"

using std::vector;

/*
template<class S>
vector<double> BinaryLogisticClassifer<S>::prob_vec(const vector<S>& in)
{
	double p0 = logistic(weights * in + bias);
	return { p0, 1 - p0 };
}

*/

